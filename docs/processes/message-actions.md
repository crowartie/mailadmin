# Действия с письмами: прочитано, флажок, перенос, удаление, спам, метки, отложить, напомнить

Экран меняется сразу, а необратимые действия уходят на сервер с задержкой: в это время
«Отменить» просто перезагружает список, потому что на сервере ещё ничего не случилось.

```mermaid
flowchart TD
    A["Кнопка, меню, клавиша, перетаскивание"] --> SCR["Экран обновлён сразу: счётчики, строки убраны"]
    SCR --> K{"Действие необратимое? удалить, архив, спам, перенос"}
    K -->|нет| POST
    K -->|да| U{"Окно отмены включено?"}
    U -->|нет| TR{"Удаление из Корзины?"}
    TR -->|да| CF["Подтверждение"] --> POST
    TR -->|нет| POST
    U -->|да| WAIT["Ждём N секунд"]
    WAIT -->|Отменить| RELOAD["Перезагрузить список"]
    WAIT -->|время вышло или уход со страницы| POST["POST /mail/api/action"]

    POST --> RIGHTS{"Есть права на папку? MYRIGHTS"}
    RIGHTS -->|нет| E409["409 «папка только для просмотра»"]
    RIGHTS --> OP{"Что делаем"}
    OP -->|прочитано, флажок| FL["UID STORE флагов пачками по 200"]
    OP -->|удалить| D{"Мы в Корзине?"}
    D -->|да| EX["флаг Deleted и EXPUNGE"]
    D -->|нет| MV
    OP -->|перенос, архив, спам, не спам| MV["UID MOVE; из общей папки в «свои» системные — в папку владельца"]
    OP -->|метка| LB["Ключевое слово Lbl_N"]
    OP -->|отложить| SN["В папку Snoozed + webmail_snoozes"]
    OP -->|напомнить| RM["webmail_reminders по Message-ID"]
    MV --> TI["Указатель переписок обновлён"]
    MV --> ASK{"Перенос из Входящих в свою папку?"}
    ASK -->|да, и правила ещё нет| RULE["Предложить правило для отправителя"]
```

```mermaid
flowchart LR
    C["mail:wake каждую минуту, master-вход"] --> S1["Пришло время отложенного письма → во Входящие, непрочитанным и с флажком"]
    C --> S2["Пришло время напоминания → ответа нет? → письмо-напоминание во Входящие"]
```

## Код

Клиент: `mail/useMessageActions.js`. Сервер: `Mail/Api/ActionController`, `MailActions`
(`flag`, `move`, `delete`), `FolderTree::rolePath`, задача `MailWake`.

Перенос в «Спам» и обратно дополнительно обучает SpamAssassin (imapsieve на стороне Dovecot).
В чужой общей папке «Отложить» недоступно.
