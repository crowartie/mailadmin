# Вход в веб-почту, 2FA, вход администратора в ящик, выход

Пароль проверяет сам Dovecot: приложение пробует войти по IMAP. Своих паролей
сотрудников приложение не хранит, в сеансе лежит только зашифрованная копия для работы с IMAP.

```mermaid
flowchart TD
    L["Форма входа /mail/login"] --> DOM["Логин без @ получает домен по умолчанию"]
    DOM --> RL{"Неудачных попыток: 8 на ящик или 60 с адреса за 15 минут?"}
    RL -->|да| BL["Отказ, запись blocked"]
    RL --> PB{"Вход сотруднику закрыт в админке?"}
    PB -->|да| BL
    PB --> IM{"IMAP-вход в Dovecot удался?"}
    IM -->|нет| BAD["«Неверный адрес или пароль», запись в журнал входов, auth.log для fail2ban"]
    IM --> TF{"Включена 2FA?"}
    TF -->|да| CH["Страница кода"]
    CH --> CODE{"Код верный? не больше 8 ошибок"}
    CODE -->|нет| BADC["Ошибка кода или блок"]
    CODE --> OK
    TF -->|нет| OK["Сеанс: mail.user и зашифрованный пароль"]
    OK --> DEV["Запись сеанса и устройства"]
    DEV --> NEW{"Новое устройство?"}
    NEW -->|да| NOTE["Письмо сотруднику о входе, если включено"]
    NEW --> REQ
    NOTE --> REQ{"Администратор требует 2FA?"}
    REQ -->|да, не настроена| FORCE["Только страница «Безопасность» до настройки 2FA"]
    REQ -->|нет| INBOX["Почта"]
```

```mermaid
flowchart LR
    ADM["Админка: сотрудник, «Войти в ящик»"] --> MU["IMAP-вход как сотрудник*master"]
    MU --> SES["Сеанс с признаком mail.master, журнал действий администратора"]
    SES --> MAIL["Веб-почта сотрудника"]
    MAIL --> SMTP["Отправка через 127.0.0.1:25 без логина"]
    MAIL --> NOACT["В «Активности» такие действия отмечены и по умолчанию не считаются"]
```

## Код

| Шаг | Где |
|---|---|
| Вход, код 2FA, выход | `Mail/LoginController` (`store`, `verifyCode`, `complete`, `destroy`) |
| Проверка пароля | `ImapSession::verify`, `ImapSession::login` |
| Каждый запрос | `EnsureMailSession`: есть ли `mail.user`, отметка сеанса, режим «только безопасность» |
| Журнал входов, блокировки | `MailLogin` (`mail_logins`) |
| Сеансы, выход с других устройств | `MailSession`, `Server/Sessions` (`doveadm kick` через `mailadmin-ctl`) |
| 2FA, пароли приложений | `Mail/Api/SecurityController` |
| Вход администратора в ящик | `EmployeeController::impersonate`, `ImapSession::loginAs` |

Ключи сеанса: `mail.user`, `mail.secret` (зашифрованный пароль), `mail.master`, `mail.force2fa`,
`mail.pending` (ждёт кода), `mail.2fa.setup`.

Если пароль сотрудника сменили, следующий запрос к IMAP получает AUTHENTICATIONFAILED:
сеанс сбрасывается, клиент показывает вход заново.
