# Облако для больших вложений

Файлы больше порога не кладутся в письмо, а публикуются на своём сервере; в письмо уходит
блок со ссылками. Ссылка продлевается, файл по умолчанию не удаляется. Скачивание отдаёт
сам nginx, приложение только проверяет токен.

```mermaid
flowchart TD
    UP["Файл при отправке: помечен «ссылкой» или больше порога"] --> SZ{"Пустой? больше предела?"}
    SZ -->|да| EX["Ошибка словами: «не догрузился» или «слишком большой»"]
    SZ --> QT{"Квота сотрудника не превышена?"}
    QT -->|превышена| EX
    QT --> AV["clamdscan"]
    AV -->|вирус| EV["«Не пропустил антивирус»"]
    AV --> TOK["Токен 32 случайных байта"]
    TOK --> MV["storage/app/files/ГГГГ/ММ/токен, sha256 до и после переноса"]
    MV -->|не совпало| EU["Ошибка, файл не опубликован"]
    MV --> ROW["webmail_files: владелец, имя, размер, хэш, письмо, срок ссылки"]
    ROW --> LINK["Ссылка https://files.домен/токен/имя в блоке письма"]
    LINK -->|письмо не ушло| RB["Откат: файлы этого письма удалить"]
```

```mermaid
flowchart TD
    GET["Получатель открыл ссылку"] --> NG["nginx files.домен → /f/токен/имя"]
    NG --> FC["FilesController: не больше 120 запросов в минуту"]
    FC --> FND{"Токен есть?"}
    FND -->|нет| G404["Страница «файла нет»"]
    FND --> EXP{"Срок ссылки истёк?"}
    EXP -->|да| G410["410 «попросите отправителя продлить»"]
    EXP --> XA["Счётчик скачиваний, X-Accel-Redirect /_files/путь"]
    XA --> NGX["nginx отдаёт файл сам: докачка, CSP sandbox, nosniff"]
```

```mermaid
flowchart LR
    N1["04:30 files:purge"] --> P1["Удаляет просроченные дольше keep_days; по умолчанию 0 — ничего"]
    N2["04:35 files:check"] --> P2["Файлы на месте и нужного размера; лишние на диске старше суток — прочь"]
    N3["вс 04:50 files:check --hash"] --> P3["Полная сверка контрольных сумм"]
    N4["Мои файлы в настройках"] --> P4["Список, продление, удаление своих"]
```

## Код

`Services/Cloud/Cloud` (выбор: своё хранилище или Nextcloud), `Services/Cloud/LocalFiles`
(`publish`, `scan`, `discardTokens`, `discardForMessage`, `check`, `purge`, `cardsIn`),
`Mail/FilesController`, `Mail/Api/CloudFilesController`, `Models/Webmail/CloudFile`,
задачи `FilesPurge`, `FilesCheck`; nginx — `deploy/nginx-files.conf.tpl`, `deploy/files-host.sh`.

Настройки («Файлы и облако» в админке): порог, предел файла, срок ссылки, предел просмотра,
квота сотрудника, имя сайта файлов.
