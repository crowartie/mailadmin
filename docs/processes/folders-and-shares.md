# Папки и общий доступ

Папки живут в Dovecot. Приложение следит за тем, чего Dovecot сам не делает: переносит
подписки при переименовании, тянет за папкой правила, раздаёт права на новые папки
общего ящика.

```mermaid
flowchart TD
    subgraph create["Создание"]
        C1["Имя: без точки, до 80 знаков, не дубль"] --> C2["IMAP CREATE в UTF-7"] --> C3["SUBSCRIBE: иначе Outlook папку не увидит"]
    end
    subgraph rename["Переименование"]
        R1{"Системная папка?"} -->|да| RX["422"]
        R1 -->|нет| R2["Запомнить вложенные папки"] --> R3["IMAP RENAME в UTF-7"]
        R3 --> R4["Перенос подписок: папка и все вложенные"]
        R4 --> R5["RuleFolders: пути в правилах заменить, скрипт Sieve перевыпустить"]
    end
    subgraph delete["Удаление"]
        D1{"Есть вложенные?"} -->|да| DX["422 «сначала удалите вложенные»"]
        D1 -->|нет| D2["Письма → Корзина"] --> D3["DELETE и UNSUBSCRIBE"]
        D3 --> D4["RuleFolders: правила в эту папку выключить"]
    end
```

```mermaid
flowchart TD
    O["Владелец: «Доступ к папке» — читатель, редактор, владелец"] --> G["guardOwn: папка своя и существует"]
    G --> ACL["mailadmin-ctl acl-set → doveadm acl set"]
    ACL --> INB{"Это Входящие?"}
    INB -->|да| REST["Права на остальные папки ящика: владельцу — все, редактору — системные"]
    INB -->|нет| DONE
    REST --> DONE["Сброс кэшей «от чьего имени можно писать»"]
    DONE --> SEE["Получатель видит Shared/владелец/… в своём списке папок"]
    CRON["shares:sync каждые 10 минут"] --> NEWF["Права на папки, созданные после выдачи доступа"]
    SEE --> RD{"Открыл письмо в чужой папке"}
    RD --> NS["Не отмечается прочитанным — флаг общий на всех; кнопка «Прочитано» есть"]
```

## Код

`Mail/Api/FolderController`, `FolderTree` (`createFolder`, `renameFolder`, `resubscribe`, `deleteFolder`,
`unmangle`), `MailStore::deleteFolder`, `RuleFolders`, `FolderShares`, задача `SharesSync`,
в админке — `SharesController`. Уборка подписок: `php artisan mail:fix-subscriptions [--apply]`.

Уровни доступа: читатель (смотреть, отмечать прочитанным), редактор (плюс переносить,
удалять, писать в папку), владелец (всё, включая отправку от имени ящика; только для Входящих).
