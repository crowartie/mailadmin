# mailadmin: время ответа по запросам веб-почты — чтобы мерить реальную скорость у сотрудников
log_format mailadmin_timed "$remote_addr $request_method $uri $status $request_time $upstream_response_time $body_bytes_sent";
